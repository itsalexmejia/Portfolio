import * as React from "react"
import { Link } from 'gatsby'

const CardMaster = props => {
    return (
        <div>
            <p>{props.title}</p>
        </div>
    )
}

export default CardMaster